import styled from 'styled-components';

const StyledMenu = styled.div`
	background: #373740;
    grid-column-start: 1;
    grid-column-end: -1;
`;

const Menu = () => (
	<StyledMenu>
		test
	</StyledMenu>
);

export default Menu;